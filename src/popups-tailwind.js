/*

 
 sketch.UI.message()
 sketch.UI.alerts('title','tekst')
 const string = sketch.UI.getStringFromUser("text", 'input')
 
 const options = ['Sketch','Sketch 2','Sketch 3','Sketch 4'];
 const selection = sketch.UI.getSelectionFromUser(
   "What's your favorite design tool?",
   options
 )
 // True or False als er iets geselecteerd is
 const selected = selection[2]
 // Option value
 const value = options[selection[1]]
*/

